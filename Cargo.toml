[package]
name = "xplane-doctor"
version = "0.1.0"
edition = "2021"

[features]
default = []
gui = ["dep:eframe", "dep:rfd"]

[dependencies]
eframe = { version = "0.29", optional = true }
rfd = { version = "0.15", optional = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "xplane-doctor"
path = "src/main.rs"

[[bin]]
name = "xplane-doctor-gui"
path = "src/bin/gui.rs"
required-features = ["gui"]

[profile.release]
strip = true
lto = true
codegen-units = 1
